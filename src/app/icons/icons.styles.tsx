import { Theme } from '@material-ui/core';

export const iconStyles = (theme: Theme): any => ({
  root: {
    flexGrow: 1,
  },
  iconContainer: {
    paddingBottom: 15,
  },
  iconGrid: {
    textAlign: 'center',
    marginBottom: 15,
  },
});
