import { Theme } from '@material-ui/core';

export const iconPageStyles = (theme: Theme): any => ({
  root: {
    flexGrow: 1,
  },
  gridContainer: {
    justifyContent: 'center',
  },
  iconGrid: {
    textAlign: 'center',
    marginBottom: 25,
  },
});

export const iconStyle: any = {
  fontSize: 50,
};

